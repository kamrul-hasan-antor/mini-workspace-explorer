const Header = () => {
  return (
    <div className="bg-[var(--bg-header)] h-8 flex items-center justify-center px-4 border-b border-b-[var(--bg-header)]">
      <h1 className="font-medium text-white">Mini Workspace Explorer</h1>
    </div>
  );
};

export default Header;
